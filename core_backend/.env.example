# ── Database ──────────────────────────────────────────────────────────────────
# Local dev (SQLite, không cần PostgreSQL):
DATABASE_URL="sqlite:///./test.db"
# PostgreSQL (production / Docker):
# DATABASE_URL="postgresql://user:password@localhost:5432/smart_travel_db"

# ── AI Engine ─────────────────────────────────────────────────────────────────
# Local dev (chạy trực tiếp bằng uvicorn):
AI_ENGINE_BASE_URL="http://localhost:8001"
# Docker Compose (service name resolution):
# AI_ENGINE_BASE_URL="http://ai_engine:8001"
