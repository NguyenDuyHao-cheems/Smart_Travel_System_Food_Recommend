# ── Frontend ──────────────────────────────────────────────────────────────────
# Local development: http://127.0.0.1:8000
# Docker Compose:    http://core_backend:8000 (internal) — but NEXT_PUBLIC_ vars
#                    are baked at build time, so browser still uses localhost:8000
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_OPENWEATHER_API_KEY=

