'use client';
import React from 'react';
// Left sidebar của trang Feed — hiện trống để phát triển chức năng social
// Có thể thêm: danh sách bạn bè online, hashtag theo dõi, bộ lọc feed, v.v.

export function FeedLeftSidebar() {
  return (
    <aside className="sticky top-24 flex flex-col gap-3 h-[calc(100vh-6rem)]">
      {/* Placeholder — thêm các widget social tại đây */}
    </aside>
  );
}
